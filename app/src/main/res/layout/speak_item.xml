<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@layout/list_bg">
    <ImageView
        android:id="@+id/speaker_img"
        android:background="@drawable/x01_alavilli_r5"
        android:layout_width="100dp"
        android:layout_height="100dp" />
    <TextView
        android:id="@+id/speaker_name"
        android:textSize="22sp"
        android:textColor="#FFFFFF"
        android:layout_toRightOf="@+id/speaker_img"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />
    <TextView
        android:id="@+id/speaker_title"
        android:textSize="15sp"
        android:textColor="#FFFFFF"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@+id/speaker_img"
        android:layout_below="@+id/speaker_name" />
</RelativeLayout>